<Window x:Class="UniAcamanageWpfApp.Windows.TimeSlotEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑时间段" Height="300" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 时间选择控件将在代码中动态添加 -->
        <StackPanel x:Name="TimeSelectionPanel" Grid.Row="0"/>

        <!-- 按钮 -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="保存" Click="SaveButton_Click" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Background="#4CAF50" Foreground="White" Margin="0,0,10,0"/>
            <Button Content="取消" Click="CancelButton_Click" Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>
    </Grid>
</Window>